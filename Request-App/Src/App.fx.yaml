App As appinfo:
    BackEnabled: =true
    OnStart: |-
        =ClearCollect(
            Adminlist,
            'Admin list'
        )
    Theme: =PowerAppsTheme

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

